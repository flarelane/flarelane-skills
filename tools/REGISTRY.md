# Tools Registry

This directory is reserved for shared tools and cross-skill references.

Use `tools/` only when a utility or reference is shared by multiple skills. Keep single-skill material inside that skill's own directory.

## Planned Uses

- shared validation helpers
- shared SDK mapping tables
- shared integration checklists
- reusable scripts referenced by multiple FlareLane skills

## Current State

No shared tools are committed yet.
